services:

  web:
    image: super-safi
    build: .

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    command: python backend/manage.py runserver 0.0.0.0:8000

    depends_on:
      - redis

  redis:
    image: redis:7-alpine